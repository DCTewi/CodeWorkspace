#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	int now = 0, ind = 0;
	while (now != 250)
	{
		ind++;
		cin>>now;
		if (now == 250)
		{
			cout<<ind<<endl;
		}
	}
	return 0;
}
