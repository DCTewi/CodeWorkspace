//A
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long long LL;

int main()
{
    //ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    
    string a, b;
    cin>>a>>b;
    
    if (a.size() >= b.size()) puts("go");
    else puts("no");
    
    return 0;
}
