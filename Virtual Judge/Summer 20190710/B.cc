// B - Flipping Game 
#include <bits/stdc++.h>
using namespace std;

const int MOD = 998244353;

int n, k, m;

int main()
{
    ios::sync_with_stdio(false);
    int T; cin >> T;
    while (T--)
    {
        cin >> n >> k >> m;
    }
    return 0;
}