#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

const int MAXN = 1e5 + 5;
int a[MAXN];

int main()
{
    ios::sync_with_stdio(0);

    int T; cin >> T;
    while (T--)
    {
        int n; cin >> n;
        vector<pair<int, int>> v;
        for (int i = 0; i < n; i++)
        {
            cin>>a[i];
        }

        for (int i = 1; i < n; i++)
        {
            
        }

        sort(v.begin(), v.end());
    }

    return 0;
}