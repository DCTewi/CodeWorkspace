#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    int n; cin >> n;
    for (int i = 0; i < n; i++)
    {
        string s; int t;
        cin >> s >> t;
        if (t == 3)
        {
            cout << s << "\n";
        }
    }

    return 0;
}
